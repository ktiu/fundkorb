---
layout: blank
---
[ 
  {% for eintrag in site.data.glossar %}{ 
    "schlagwort": "{{ eintrag.schlagwort }}",
    {% if eintrag.synonym %} "synonym": "{{ eintrag.synonym }}",{% endif %}
    "definition": "{{ eintrag.definition }}"
  }{% unless forloop.last %},{% endunless %}
  {% endfor %}
]
